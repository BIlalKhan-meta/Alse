// components/OrderListComponent/styles.ts
import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    ordersContainer: {
        paddingHorizontal: 10,
        paddingVertical: 10,
    },
});

export default styles;
