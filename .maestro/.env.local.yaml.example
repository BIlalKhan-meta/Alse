# Copy to .env.local.yaml and fill in staging credentials.
# Account should have has_subscription to avoid Subscribe gates on Home/Marketplace.
env:
  TEST_EMAIL: "your-staging-user@example.com"
  TEST_PASSWORD: "your-password"
